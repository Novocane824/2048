// Generated by CoffeeScript 1.7.1
(function() {
  $(function() {
    var boardFull, cells, collapseCells, generateTile, getColumn, getRandomCell, getRow, mergeCells, ppArray, randomInt, randomValue, result, x, y, _i, _j;
    ppArray = function(array) {
      var row, _i, _len, _results;
      _results = [];
      for (_i = 0, _len = array.length; _i < _len; _i++) {
        row = array[_i];
        _results.push(console.log(row));
      }
      return _results;
    };
    this.board = [0, 1, 2, 3].map(function() {
      return [0, 1, 2, 3].map(function() {
        return 0;
      });
    });
    for (x = _i = 0; _i <= 3; x = ++_i) {
      this.board[x] = [];
      for (y = _j = 0; _j <= 3; y = ++_j) {
        this.board[x][y] = 0;
      }
    }
    randomInt = function(x) {
      return Math.floor(Math.random() * x);
    };
    getRandomCell = function() {
      return [randomInt(4), randomInt(4)];
    };
    randomValue = function() {
      var val, values;
      values = [2, 2, 2, 4];
      return val = values[randomInt(values.length)];
    };
    boardFull = function(board) {
      var _k, _l;
      for (x = _k = 0; _k <= 3; x = ++_k) {
        for (y = _l = 0; _l <= 3; y = ++_l) {
          if (board[x][y] === 0) {
            return false;
          }
        }
      }
      return true;
    };
    generateTile = function(board) {
      var val, _ref;
      if (!boardFull(board)) {
        val = randomValue();
        _ref = getRandomCell(), x = _ref[0], y = _ref[1];
        if (board[x][y] === 0) {
          return board[x][y] = val;
        } else {
          return generateTile(board);
        }
      }
    };
    console.log(generateTile(this.board));
    $('body').keydown(function(e) {
      var key, keys;
      key = e.which;
      keys = [37, 38, 39, 40];
      if ($.inArray(key, keys) > -1) {
        e.preventDefault();
      }
      switch (key) {
        case 37:
          return console.log('left');
        case 38:
          return console.log('up');
        case 39:
          return console.log('right');
        case 40:
          return console.log('down');
      }
    });
    generateTile(this.board);
    ppArray(this.board);
    getRow = function(r, board) {
      return board[r];
    };
    getColumn = function(c, board) {
      var b;
      b = board;
      return [b[0][c], b[1][c], b[2][c], b[3][c]];
    };
    mergeCells = function(originalCells, direction) {
      var cells, _k, _l, _m, _n, _ref, _ref1;
      cells = originalCells;
      switch (direction) {
        case 'left':
        case 'up':
          for (x = _k = 0; _k < 3; x = ++_k) {
            for (y = _l = _ref = x + 1; _ref <= 3 ? _l <= 3 : _l >= 3; y = _ref <= 3 ? ++_l : --_l) {
              if (cells[x] === 0) {
                break;
              } else if (cells[x] === cells[y]) {
                cells[x] *= 2;
                cells[y] = 0;
                break;
              } else if (cells[y] !== 0) {
                break;
              }
            }
          }
          break;
        case 'right':
        case 'down':
          for (x = _m = 3; _m > 0; x = --_m) {
            for (y = _n = _ref1 = x - 1; _ref1 <= 0 ? _n <= 0 : _n >= 0; y = _ref1 <= 0 ? ++_n : --_n) {
              if (cells[x] === 0) {
                break;
              } else if (cells[x] === cells[y]) {
                cells[x] *= 2;
                cells[y] = 0;
                break;
              } else if (cells[y] !== 0) {
                break;
              }
            }
          }
      }
      return cells;
    };
    console.log("merging right: " + mergeCells([2, 0, 2, 2], 'right'));
    console.log("merging left : " + mergeCells([2, 0, 4, 2], 'left'));
    collapseCells = function(originalCells, direction) {
      var cells, count, countZero, i, temp, _k, _l, _m;
      countZero = function(array) {
        var count, _k, _len;
        count = 0;
        for (_k = 0, _len = array.length; _k < _len; _k++) {
          x = array[_k];
          if (x === 0) {
            count = count + 1;
          }
        }
        return count;
      };
      console.log("counting 0's: " + countZero([0, 0, 0, 4]));
      count = countZero(originalCells);
      for (i = _k = 0; 0 <= count ? _k < count : _k > count; i = 0 <= count ? ++_k : --_k) {
        cells = originalCells;
        switch (direction) {
          case 'left':
          case 'up':
            for (x = _l = 0; _l < 3; x = ++_l) {
              temp = cells[x];
              if (cells[x] === 0) {
                cells[x] = cells[x + 1];
                cells[x + 1] = temp;
              }
            }
            break;
          case 'right':
          case 'down':
            for (x = _m = 3; _m > 0; x = --_m) {
              temp = cells[x];
              if (cells[x] === 0) {
                cells[x] = cells[x - 1];
                cells[x - 1] = temp;
              }
            }
        }
      }
      return cells;
    };
    console.log("collapsing: " + collapseCells([2, 0, 2, 2], 'right'));
    console.log("collapsing: " + collapseCells([2, 0, 2, 4], 'left'));
    console.log("collapsing: " + collapseCells([2, 4, 0, 4], 'left'));
    console.log("collapsing: " + collapseCells([0, 0, 0, 4], 'left'));
    cells = [2, 0, 2, 4];
    result = mergeCells(cells);
    this.board;
    return collapseCells(cells);
  });

}).call(this);

//# sourceMappingURL=main.map
